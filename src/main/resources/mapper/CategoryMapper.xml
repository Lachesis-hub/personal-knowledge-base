<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.personalkb.mapper.CategoryMapper">

    <insert id="insert"
            useGeneratedKeys="true"
            keyProperty="id">

        insert into category
        (
            user_id,
            name
        )
        values
            (
                #{userId},
                #{name}
            )

    </insert>

    <select id="selectByUserId"
            resultType="com.personalkb.entity.Category">

        select *
        from category
        where user_id = #{userId}
        order by id desc

    </select>

</mapper>