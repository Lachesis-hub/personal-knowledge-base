<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.personalkb.mapper.UserMapper">

    <select id="selectByUsername"
            resultType="com.personalkb.entity.User">

        select *
        from user
        where username = #{username}

    </select>

    <insert id="insert">

        insert into user(
            username,
            password,
            nickname
        )
        values(
                  #{username},
                  #{password},
                  #{nickname}
              )

    </insert>

</mapper>